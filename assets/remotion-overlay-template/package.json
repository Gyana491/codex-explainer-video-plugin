{
  "name": "explainer-overlay-render",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "preflight": "node scripts/preflight.mjs",
    "prerender": "npm run preflight",
    "render": "npm run render:remotion && npm run finalize",
    "render:remotion": "remotion render src/index.ts ExplainerVideo output/explainer-video-remotion.mp4",
    "finalize": "ffmpeg -y -i output/explainer-video-remotion.mp4 -vf \"scale=in_range=full:out_range=tv,format=yuv420p\" -c:v libx264 -pix_fmt yuv420p -color_range tv -c:a aac -movflags +faststart output/explainer-video.mp4",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.342",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remotion": "^4.0.342"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.7.2"
  }
}
